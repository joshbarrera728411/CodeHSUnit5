public int findMultipleOfThree(int[] arr) {
    for (int i = arr.length - 1; i >= 0; i--) {
        if (arr[i] % 3 == 0) {
            return i; // Return the index of the last multiple of three found
        }
    }
    return -1; // If no multiple of three is found, return -1
}
